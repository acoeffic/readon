
[functions.send-push-notification]
enabled = true
verify_jwt = false
import_map = "./functions/send-push-notification/deno.json"
entrypoint = "./functions/send-push-notification/index.ts"

[functions.send-streak-reminders]
enabled = true
verify_jwt = false
entrypoint = "./functions/send-streak-reminders/index.ts"

[functions.send-wrapped-notification]
enabled = true
verify_jwt = false
import_map = "./functions/send-wrapped-notification/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/send-wrapped-notification/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/send-wrapped-notification/*.html" ]

# Webhook RevenueCat → Supabase. RC ne signe pas en JWT Supabase ;
# l'auth est faite dans le code via REVENUECAT_WEBHOOK_SECRET.
[functions.revenuecat-webhook]
enabled = true
verify_jwt = false
entrypoint = "./functions/revenuecat-webhook/index.ts"

# Pont HTTPS pour OAuth Notion. Notion redirige le user vers cette
# URL puis on 302 vers le custom scheme `lexday://notion/callback`.
# Pas d'auth Supabase possible (le browser du user n'a pas de JWT).
[functions.notion-oauth-redirect]
enabled = true
verify_jwt = false
entrypoint = "./functions/notion-oauth-redirect/index.ts"
